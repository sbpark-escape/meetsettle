{
  "name": "meetsettle",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "web:dev": "npm --workspace @meetsettle/web run dev",
    "web:build": "npm --workspace @meetsettle/web run build",
    "api:run": "dotnet run --project apps/api/MeetSettle.Api.csproj",
    "test": "dotnet test"
  },
  "overrides": {
    "postcss": "8.5.10"
  }
}
